{
  "name": "sts2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run -w shared build && npm install",
    "dev:api": "npm run start -w sim_server",
    "dev:ui": "npm run dev -w sim_ui",
    "format": "prettier --write"
  },
  "devDependencies": {
    "prettier": "^3.8.4"
  }
}
