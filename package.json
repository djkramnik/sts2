{
  "name": "sts2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace sim_server",
    "start": "npm run start --workspace sim_server",
    "sim": "npm run sim --workspace sim_server"
  }
}
